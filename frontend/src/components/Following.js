import React from "react";
import { Box, Text } from "@chakra-ui/react";

const Following = () => (
  <Box bg="gray.100" p={5}>
    <Text>This is the "Following" page</Text>
  </Box>
);

export default Following;
